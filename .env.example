# ── HyperSnap node ────────────────────────────────────────────────────────────
# gRPC endpoint (for submitting and reading messages)
HYPERSNAP_GRPC=localhost:3383
# HTTP endpoint (for REST queries)
HYPERSNAP_HTTP=http://localhost:3381

# ── Farcaster identity ────────────────────────────────────────────────────────
# Your Farcaster ID (FID)
FARCASTER_FID=12345
# Farcaster network: mainnet | testnet | devnet
FARCASTER_NETWORK=mainnet

# ── Signing key (choose ONE of the two methods below) ─────────────────────────
# Option A — raw Ed25519 private key (64-char hex, no 0x prefix)
SIGNER_PRIVATE_KEY=

# Option B — BIP-39 mnemonic (12 or 24 words); key derived via SLIP-0010
SIGNER_MNEMONIC=

# ── Telegram bot ──────────────────────────────────────────────────────────────
# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Comma-separated Telegram user IDs allowed to control the bot
# Leave empty to allow everyone (not recommended for a personal bot)
TELEGRAM_ALLOWED_USERS=
