# ── HyperSnap / Snapchain node ────────────────────────────────────────────────
# Connect to any remote Snapchain-compatible node. No local installation needed.
#
# Option A — Community or self-hosted node (plain gRPC, no TLS)
HYPERSNAP_GRPC=snapchain.example.com:3383
HYPERSNAP_HTTP=http://snapchain.example.com:3381
HYPERSNAP_SSL=false
#
# Option B — Managed provider (TLS + API key, e.g. Neynar)
# HYPERSNAP_GRPC=hub.neynar.com:3383
# HYPERSNAP_HTTP=https://hub.neynar.com:3381
# HYPERSNAP_SSL=true
# HYPERSNAP_API_KEY=your-neynar-api-key
#
# Option C — Node with Basic Auth
# HYPERSNAP_USERNAME=youruser
# HYPERSNAP_PASSWORD=yourpassword

# ── Farcaster identity ────────────────────────────────────────────────────────
# Your Farcaster ID (FID)
FARCASTER_FID=2771439
# Network: mainnet | testnet | devnet
FARCASTER_NETWORK=mainnet

# ── Signing key (choose ONE) ──────────────────────────────────────────────────
# Option A — raw Ed25519 private key (64 hex chars, no 0x prefix)
# This key must already be registered on-chain via the Farcaster Key Registry.
SIGNER_PRIVATE_KEY=

# Option B — BIP-39 mnemonic (12 or 24 words); Ed25519 key derived via SLIP-0010
SIGNER_MNEMONIC=

# ── Telegram bot ──────────────────────────────────────────────────────────────
# Token from @BotFather
TELEGRAM_BOT_TOKEN=

# Comma-separated Telegram user IDs allowed to control the bot
# Leave empty to allow all users (not recommended for a personal bot)
TELEGRAM_ALLOWED_USERS=
